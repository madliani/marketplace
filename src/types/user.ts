export type BackendUser = {
  firstName: string
  id: string
  image: string
  lastName: string
}

export type User = {
  avatar: string
  balance: number
  firstName: string
  id: string
  lastName: string
}
